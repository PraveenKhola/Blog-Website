<%- include('partials/header.ejs') %>

    <% if(locals.post) { %>
        <h1><%= post.postTitle %></h1>
        <p><%= post.postContent %></p>
    <% } else {%>
        <div class="error">
            <h1>The post you are looking for is not present</h1>
            <img src="/images/notFound.avif" alt="Content not found">
        </div>
    <%}%>

<%- include('partials/footer.ejs') %>